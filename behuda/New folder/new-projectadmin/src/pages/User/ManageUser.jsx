import React from 'react';

const ManageUser = () => {
    return (
        <div>
            this is nmmanege const [state, dispatch] = useReducer(first, second, third)
        </div>
    );
};

export default ManageUser;